<?php
// need to test if the parameter number exists
$limit = $_GET['number']

$count = 0;

while($count <= $limit)
{
	echo $count.'<br>';
	$count = $count + 1;
}

for($count = 0; $count <= $limit; $count++)
}
	echo $count.'<br>';
}

